import {Text, View} from "react-native";
import ScreenWrapper from "@/components/screen-wrapper";

const Home = () => {
    return (
        <ScreenWrapper>
            <Text>Home</Text>
        </ScreenWrapper>
    );
};

export default Home;